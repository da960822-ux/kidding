# 밭머리 UI 원칙

이 문서는 제품·API 계약을 반복하지 않고, 현재 웹 UI의 표현 원칙만 정한다.

## 역할별 화면

- 농장주 화면은 **결정 확인 장부**다. 원음, 해석 결과, 시간, 안전, 모든 미확정 내용을 한곳에서 확인한 뒤 확정하거나 다시 녹음한다.
- 근로자 화면은 **한 번에 한 행동**만 제시한다. 선택한 언어로 장소·수량·시간·안전과 현재 단계를 먼저 보여주고, 기술 메타데이터는 노출하지 않는다.
- `AppShell` 하나가 역할별 문구와 탐색 항목만 바꾼다. 역할별 셸을 따로 만들지 않는다.

## 가독성과 조작

- 320px부터 1024px까지 가로 스크롤 없이 읽고 조작할 수 있어야 한다.
- 본문은 큰 기본 글자와 강한 명암을 유지하되, 긴 한국어·베트남어·네팔어 문구가 좁은 열로 찌그러지지 않게 한다.
- 모든 조작 대상은 실제 44×44px 이상이며, 키보드 초점과 선택 상태를 시각적·의미적으로 함께 표시한다.
- 화면 전환 뒤 주 제목으로 초점을 이동하고, 본문 바로가기 링크를 제공한다.

## 안전·상태·탐색

- 안전과 미확정 내용은 확정 행동보다 먼저 보인다. 내부 enum 대신 사람이 이해하는 문구를 쓴다.
- 로딩은 결과 언어가 정해지기 전 특정 언어로 오인시키지 않는다. 오류는 원인과 다음 행동을 함께 말한다.
- 숨겨진 탭에서는 주기적 갱신을 멈추고, 전체 화면·카메라·음성 실패에는 복구 문구를 제공한다.
- 모바일에서 같은 목적의 탐색 장치를 중복하지 않는다. 현재 위치는 `aria-current`, 토글 선택은 `aria-pressed`로 전달한다.

## 움직임

- 움직임은 녹음, 재생, 상태 변경의 피드백에만 사용한다.
- `prefers-reduced-motion`에서는 공간 이동과 반복 애니메이션을 줄이되 상태 변화는 남긴다.
